<!doctype html>
<html lang="zh-CN">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="">
        <meta name="author" content="Eric">
        <title><%= config.siteName %></title>
        <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
        <link rel="stylesheet" href="/toastr/toastr.min.css">
        <link rel="stylesheet" href="/select2/css/select2.min.css">
        <link rel="stylesheet" href="/select2/css/bootstrap4-theme.css">
        <link rel="stylesheet" href="/css/style.css?v=<%= config.assetVersion %>">
    </head>
    <body class="main">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
            <a class="navbar-brand" href="/"><%= config.siteName %></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link<% if (router == 'home') { %> active<% } %>" href="/home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link<% if (router == 'command') { %> active<% } %>" href="/command">Command</a>
                    </li><% if (user.role == "Admin") { %>
                    <li class="nav-item">
                        <a class="nav-link<% if (router == 'account') { %> active<% } %>" href="/account">Account</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link<% if (router == 'config') { %> active<% } %>" href="/config">Config</a>
                    </li><% } %>
                    <li class="nav-item">
                        <a class="nav-link<% if (router == 'profile') { %> active<% } %>" href="/profile">Profile</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link ajax-link" href="/user/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </nav>
        <div class="loading" style="display: none;">
            <div class="spinner-border text-warning" role="status">
                <span class="sr-only">Loading...</span>
            </div>
        </div>
        <div class="container">
